<UserControl x:Class="BusRouterUI.Views.BusRouterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BusRouterUI.Views"
             xmlns:converters="clr-namespace:BusRouterUI.Converters">
    <UserControl.Resources>
        <converters:BooleanToStringConverter x:Key="BooleanToStringConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="800"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="800"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Column="0" Grid.Row="0" Margin="30" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" FontSize="36" TextAlignment="Center" FontWeight="Bold">Bus Panel</TextBlock>
            <ItemsControl Grid.Row="1" ItemsSource="{Binding Buses}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,20">
                            <TextBlock FontSize="28" Margin="0,0,0,10" TextAlignment="Center" Text="{Binding Name}"/>
                            <TextBlock FontSize="18" Margin="0,0,0,10" Text="{Binding AmountOfSeats, StringFormat='Amount Of Seats: {0}'}" />
                            <ListView ItemsSource="{Binding StopStatistics}">
                                <ListView.Resources>
                                    <Style TargetType="GridViewColumnHeader">
                                        <Setter Property="IsHitTestVisible" Value="False"/>
                                    </Style>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    </Style>
                                </ListView.Resources>
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="Stop" DisplayMemberBinding="{Binding StopName}"/>
                                        <GridViewColumn Header="People" DisplayMemberBinding="{Binding PeopleAmount}"/>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
        
        <Canvas Grid.Column="1" Grid.Row="0">
            <Canvas.Background>
                <ImageBrush ImageSource="pack://application:,,,/Resources/Images/Map.png"/>
            </Canvas.Background>
            <ItemsControl ItemsSource="{Binding Buses}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style>
                        <Setter Property="Canvas.Left" Value="{Binding Left}" />
                        <Setter Property="Canvas.Top" Value="{Binding Top}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Image Source="{Binding ImagePath}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Canvas>

        <Grid Grid.Column="2" Grid.Row="0" Margin="35,20" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" FontSize="36" TextAlignment="Center" FontWeight="Bold">Stop Panel</TextBlock>
            <ItemsControl Grid.Row="1" ItemsSource="{Binding Stops}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,20">
                            <TextBlock FontSize="28" Margin="0,0,0,10" TextAlignment="Center" Text="{Binding Name}"/>
                            <ListView ItemsSource="{Binding BusStatistics}">
                                <ListView.Resources>
                                    <Style TargetType="GridViewColumnHeader">
                                        <Setter Property="IsHitTestVisible" Value="False"/>
                                    </Style>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    </Style>
                                </ListView.Resources>
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="Bus" DisplayMemberBinding="{Binding BusName}"/>
                                        <GridViewColumn Header="People" DisplayMemberBinding="{Binding PeopleAmount}"/>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
        
        <Grid Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Command="{Binding StartOrRestartCommand}">
                <TextBlock FontSize="32" Text="{Binding IsStarted, Converter={StaticResource BooleanToStringConverter}}"/>
            </Button>
            <Button Grid.Column="1" Command="{Binding StopCommand}">
                <TextBlock FontSize="32">Stop</TextBlock>
            </Button>
        </Grid>
    </Grid>
</UserControl>